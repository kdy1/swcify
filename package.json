{
  "name": "@shopify/swcify",
  "version": "0.1.3",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@napi-rs/cli": "^1.0.0",
    "jest": "^27.1.0"
  },
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "swcify",
    "triples": {
      "defaults": true
    }
  },
  "scripts": {
    "test": "yarn jest",
    "prepublishOnly": "yarn build",
    "build": "napi build --platform --release native",
    "build:test": "napi build --platform native",
    "build:debug": "napi build --platform native"
  },
  "dependencies": {
    "@node-rs/helper": "^1.2.1"
  }
}
